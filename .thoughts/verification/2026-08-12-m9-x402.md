# M9 x402 — live verification (Coston2), 2026-08-12

**Result: PASS (AC3).** A real x402 loop completed on Coston2: `402 → sign EIP-3009 →
settle → resource`. The **settlement** (a facilitator transaction) and the **resource**
(HTTP 200 + body) were recorded as two independent facts. The asset was MockUSDT0,
**labelled a demo token** throughout. `succeeded` was reached only from the observed
settlement + delivery.

## Network + actors

- Network: Flare Testnet **Coston2** (chain 114).
- Payer (client, holding minted MockUSDT0): `0xDddF991858311597bFD3D125cb342a0d4B56ea0a`.
- Payee (the server's receiving address): `0xA4b05cdB545FA7CA12Be9f866d64E8A843A31Bd9`.
- MockUSDT0 (demo token): `0x2dA725841FF6F5367E65C5d114aa66C034A3d97b` (verified on the explorer).
- X402Facilitator: `0x57da665Ef6Bd39F82Af6BC0764cd779E9C156DdA` (verified on the explorer).

## The loop

1. `GET /api/demo` → **402** with a payment requirement: `0.1 mUSDT0 (demo)` to the payee,
   `demoToken` derived from the kit's registry (`true`), not the server's word.
2. The payer signed an EIP-3009 `TransferWithAuthorization` **off-chain (0 gas)**.
3. Re-request with the base64 `X-Payment` header → the server verified + settled via the
   facilitator (server pays the gas) and returned `200` + the resource.

## Two independent facts

| Fact | Evidence |
| --- | --- |
| **Settlement** (on-chain) | tx `0x2923aa74b5478b3cf6b5aa6e279acfdf120ece201afd8de8f0701d076e698158` (status **success**); paymentId `0x3ef50c3d33ff5bd92b288c8427164b1e59abdf1df77e6206e0111903927843cc`; mUSDT0 **payer −0.1, payee +0.1**. |
| **Resource** (HTTP) | `200 OK`; body `{ demo: true, note: "Synthetic x402 demo resource — no real data…", servedAt, settlement }` — no fabricated data (the tutorial's `flarePrice` deliberately not copied). |

Explorer: https://coston2-explorer.flare.network/tx/0x2923aa74b5478b3cf6b5aa6e279acfdf120ece201afd8de8f0701d076e698158

## The lifecycle traverses honestly

`submitted → awaiting_external` (actor **provider**) `→ succeeded`.

- `succeeded` was entered ONLY from **settled + delivered** (`reconcileX402`).
- Settlement and resource are two independent states. **settled + resource-failed →
  `partially_succeeded`** (never `succeeded`) is unit-tested in
  `packages/core/test/x402-states.test.ts`; the reference fixture delivers the resource
  atomically with the settlement response, so that split is not live-reproducible here.

## Gate

`x402Verified` for Coston2 flipped `false → true` in `packages/contracts/src/x402.ts`
ONLY after the confirmed settlement read. The contracts registry test now asserts `true`
(7 passed). The server imported the EIP-3009 crypto from `@flare-kit/core`
(`recoverAuthorizationSigner`, `eip3009Domain`) — no re-declaration; the operator key was
read from `X402_PRIVATE_KEY` and appears in no log or response. Raw data:
`2026-08-12-coston2-live-x402.json` (no key material).
