# Verification: flare-kit.xyz docs site — full documentation

Date: 2026-08-13
Spec: `.thoughts/specs/2026-08-13-docs-site.md`
Branch: `docs-and-branding` (commits `a870b8c` → `334176b` + the a11y fix)

## What shipped

78 documentation pages on one authored prev/next reading chain:

- Getting started (4): Introduction, Quickstart, Theming, Mock mode
- Components (40): all 39 composed `@flare-kit/react-ui` exports plus a
  Primitives reference (primitives share one page; `FeedCatalogueRow` is
  documented on the FeedCatalogue page)
- Hooks (23): FlareProvider, all 21 hooks, the operation store — each hook's
  Live pane is a `HookReadout` of the hook's actual return value running
  against the mock kit on that render
- Headless (5): lifecycle, states, adapters, storage/reconcile, mock
- Contracts (2): networks/registries, and Verified flags whose table renders
  the real `*Verified` booleans imported from `@flare-kit/contracts` at
  render time (R10)
- Agent Kit (4): tools, MCP server, CLI, Skills — every page states
  **Planned** and cites `.thoughts/decisions/2026-08-03-agent-facing-surfaces.md`
  (R9)

Component previews mount the gallery's own cases via the `@gallery/*` alias
behind a state switcher (R5/R6 — no re-authored fixtures; the gallery imports
`@flare-kit/react-ui` by package name so Turbopack compiles it).

## Acceptance criteria

**AC1 — full gate green.** `pnpm build && pnpm typecheck && pnpm lint && pnpm test`
run from the root after the final commit: build 5/5, typecheck 11/11, lint
clean, tests — contracts 176/2 skip, core 962, react 57, react-ui 385,
relayer 7, x402-server 12, site 15. Site build generates 83 static routes;
Pagefind indexes 78 pages / 4,399 words.

**AC2 — previews render, states reachable.** Browser-verified (Chrome via
devtools MCP, light and dark): MintFXRP (8-state switcher driven to the
below-minimum refusal), ClaimCard (15 states incl. the `unofficial mirror`
source chip), Verified flags, Agent tools, usePortfolio, Theming, Mock mode,
plus the landing at 1280/500px. Every one of the 78 pages fetched and
returned 200 with its demo markup. Switcher-equals-gallery is enforced by
test (`preview.test.tsx`: options must equal the gallery cases one-for-one).

**AC3 — search on body-only text.** Pagefind query `turnoutBIPS` (appears
only in useAnchorProof's body) returns exactly `/docs/hooks/use-anchor-proof`.

**AC4 — llms + markdown source.** `/llms.txt` returns the 86-line index;
`/llms-full.txt` returns 476 KB; a page's markdown is served (HTTP 200) and
every page carries the Copy page control.

**AC5 — planned pages.** All four Agent Kit pages contain the Planned banner
and the decision path (verified across all pages by sweep).

**AC6 — axe, both themes.** axe-core 4.x injected into the real browser:
landing page — 0 violations light, 0 dark; ClaimCard page — 0 light, 0 dark.
Two violations were found and fixed rather than suppressed, both measured
against computed styles:
- `fk-chip-ok` / `fk-chip-att` small text on their soft tints measured
  4.34:1 / 4.40:1 (light) — under AA. Fixed by extending the `brandText`
  rule: new `--fk-success-text` (#006b34) and `--fk-attention-text`
  (#8a5200) carry small text on tints, measured 5.71:1 / 5.51:1. DESIGN.md
  records the tokens and measurements. Dark already cleared 9:1; its -text
  values equal the dark hues.
- The docs code window's horizontal scroll region was not keyboard-reachable
  (`scrollable-region-focusable`); it now takes `tabIndex={0}` (no landmark
  role — many code windows per page, and same-named regions are their own
  violation).

**AC7 — nothing unreal and unlabelled.** Every component page's preview is
the gallery's own mock-driven cases and carries the `mock kit` label
(verified across all component pages by sweep; the Primitives reference
mounts no protocol values). Hook readouts serialize what the hook actually
returned. The Verified flags table reads the registry at render time — a
value on that page cannot be typed in.

## Commands and evidence

- Sweep script (all pages, Planned banners, mock labels): recorded in this
  session; result `ok: 78/78`, no missing banners, no unlabelled component
  previews.
- Screenshots: `.thoughts/verification/site-screens/docs-branding/` —
  landing (dark/light/mobile), mint-fxrp (light full, dark + driven state),
  theming (dark), mock-mode (dark), claim-card (light full), verified-flags,
  agent-tools.
- Hydration: consoles clean on the sampled pages (an earlier hydration error
  on ClaimCard was a dev-server hot-reload artifact while agents wrote
  files; it does not reproduce after a fresh build).

## Honest limits

- AC2's full 78-page × every-state browser pass was sampled, not exhaustive:
  every page was fetched and returned 200 with demo markup, and the
  state-switcher ⇄ gallery-case equivalence is enforced by test, but only
  the pages listed above were driven by hand in the browser both themes.
- axe ran on the landing plus one component page (the spec's AC6 bar), not
  all 78.

## Review gate (applied 2026-08-13, after the AC pass)

Three subagents — simplification, honesty/vocabulary, correctness-vs-source —
reviewed the batch. All findings fixed and re-gated (build, typecheck, lint,
all tests green):

- **Simplifier** (4 important, 4 minor): one shared `MockKitProvider` replaces
  eight verbatim provider wrappers; `Preview` left the MDX map; `CodeBlock`
  lost a never-passed prop; a duplicate gallery import merged; three inline
  grids became `.demo-stack`; two exports went module-local; an impossible
  clamp dropped. Explicitly clean: no hand-rolled switchers/serializers, all
  registry exports used, no file over 300 lines.
- **Honesty** (no critical; 2 important, 2 minor): the `createMockKit`
  provenance claim was wrong for the read surfaces — mock-mode and six
  component pages now say which source drove each preview, RouteCatalogue's
  chip reads `observed fixtures`, the useAttestation demo discloses its
  compressed round clock on screen, the one live amount in prose got mono +
  full precision, and the incentive demo pins its readings to their record.
  Explicitly clean: no success-language on submitted, no "failed" on unknown
  outcomes, planned surfaces all honest, no invented hashes or balances.
- **Correctness** (2 critical, 7 important/minor — all in prose; the API
  surface was exhaustively clean: every import, every props/parameters row,
  every default, every gallery section id verified by sweep). Fixed: the
  false "undelegate first" remedy for mode-conflict — the chain never resets
  delegation mode, so the style is fixed per account — corrected in the kit's
  own UI copy, its pinned test, the gallery, two source comments and both
  docs pages; feed-detail's "proven" for an unverified anchor; useAttestation
  error capture scoped to FlareKitError; ClaimCard's host-held staking
  operation; the terminal-first same-state reconcile semantics; networks'
  parity-test coverage claim narrowed to the seven re-resolved names; the
  bridge shim address correctly attributed to the spec, not the registry;
  present-tense analytics reworded as scoped-and-unbuilt (R-OBS-004); the
  hypothetical claim-card states qualified as the gallery qualifies them;
  plus the smaller wording fixes. Not taken: cosmetic `!`-style
  inconsistencies across usage fences; mock-fdc's fixture address literal
  (recorded here as a possible source cleanup, behaviour-neutral).
