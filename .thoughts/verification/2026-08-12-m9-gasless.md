# M9 gasless FXRP — live verification (Coston2), 2026-08-12

**Result: PASS (AC1 + AC2).** A real FXRP payment landed on Coston2 with the payer
paying **0 gas for the payment**, after a one-time payer-gassed approval. `succeeded`
was reached ONLY from reading the on-chain `PaymentExecuted`, never from the relayer's
HTTP response.

## Network + actors

- Network: Flare Testnet **Coston2** (chain 114), RPC `https://coston2-api.flare.network/ext/C/rpc`.
- Operator / relayer: `0xA4b05cdB545FA7CA12Be9f866d64E8A843A31Bd9` (runs the fee-free relayer, pays gas).
- Payer (AC1, a distinct account): `0xDddF991858311597bFD3D125cb342a0d4B56ea0a`.
- Recipient: the operator (payment lands back with a funded address).
- Forwarder: `0x7F358717afdEC6FD4AFEfCf2e7dD9ff3dF4b9c17` (verified on the explorer).
- FXRP (FTestXRP): `0x0b6A3645c240605887a5532109323A3E12273dc7`.

## The honest gasless proof

| Phase | What happened | Evidence |
| --- | --- | --- |
| B — one-time approval (**payer pays gas**) | payer `approve(forwarder, MaxUint256)` | tx `0x753a184f770f1b745b5cc0698bcf540833b35cc73689cadf3583233682137ca5`; payer C2FLR dropped **0.03351725** (gas). Gasless ≠ free. |
| C — drain to ~0 | payer C2FLR sent back to the operator | residual **0.001365 C2FLR** — the payer cannot self-gas the payment even if asked. |
| D — gasless payment (**payer pays 0 gas**) | payer signs a `PaymentRequest` off-chain; the relayer submits and pays gas | payment tx `0x799d3c9eb0893643d418f334cb15d97cc6b2140d541bd46cef6901938f5b02d4` (`PaymentExecuted`); **payer C2FLR 0.001365 → 0.001365, unchanged**; FXRP delta payer **−1**, recipient **+1**. |

Explorer:
- approval: https://coston2-explorer.flare.network/tx/0x753a184f770f1b745b5cc0698bcf540833b35cc73689cadf3583233682137ca5
- payment: https://coston2-explorer.flare.network/tx/0x799d3c9eb0893643d418f334cb15d97cc6b2140d541bd46cef6901938f5b02d4

## AC2 — the lifecycle traverses honestly

Driven through the kit's own state machine:

`submitted` (relay HTTP 200) → `awaiting_external` (actor **relayer**) → `succeeded`.

- After the relayer's HTTP 200 the op was **`submitted`, NOT `succeeded`**.
- `succeeded` was entered ONLY from the confirmed on-chain `PaymentExecuted` read
  (`reconcileGaslessPayment` with `transfer.kind === 'confirmed'`).
- The relay receipt (`{ accepted: true, txHash }`) never advanced the op on its own.

## Gate

`gaslessVerified` for Coston2 flipped `false → true` in `packages/contracts/src/gasless.ts`
ONLY after the confirmed transfer read. The contracts registry test now asserts `true`
(4 passed). The relayer imported the EIP-712 crypto from `@flare-kit/core` (no
re-declaration); the operator key was read from `RELAYER_PRIVATE_KEY` and appears in no
log or response. Raw data: `2026-08-12-coston2-live-gasless.json` (no key material).

## Note on Coston2 conditions

The Coston2 base gas price during the run was ~**650 gwei** (≈26× the usual ~25 gwei),
which is why the payer needed ~0.5 C2FLR funded for its one approval. This affects only
the payer-gassed approval and the relayer's gas; the payment itself cost the payer 0.
