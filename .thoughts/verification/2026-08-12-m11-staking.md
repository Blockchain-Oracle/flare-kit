# M11 staking — LIVE keyless reads (AC1) + plan/invariant proof (AC2), Coston2

- Ran: 2026-08-12T19:45:25.242Z · network **coston2** (chainId 114) · block **33979980** · account `0xA4b05cdB545FA7CA12Be9f866d64E8A843A31Bd9`
- P-address (shipped `pAddressFromPubKey`, cross-checked vs the SDK): `P-costwo1mxmg9atg27q2zav0kam8qswq32kkpvchnmnacj`
- Broadcast: **none** — KEYLESS read pass. The value-locking stake is HELD (see Carry).
- `stakeVerified`: **false** (unchanged — no live round trip has confirmed it).

## Resolved deployment (M11 registry, Coston2)

- PChainStakeMirror: `0xd2a1Bb23eB350814a30Dd6f9de78Bb2C8fdD9F1D`
- PChainStakeMirrorVerifier: `0xd61d6bEfB3B5C3e1D13b43C46CC1cebA3505a7aF`
- ValidatorRewardManager: `0x33913AcE907F682E305f36d7538D3cCd37E2cA5B`
- P-chain RPC: `https://coston2-api.flare.network/ext/bc/P` · hrp `costwo`

## Live stake limits (read, never hardcoded)

- min amount: **50000 FLR** (50000000000000000000000 wei)
- max amount: **200000000 FLR** (200000000000000000000000000 wei)
- duration: **14–365 days** (1209600–31536000 s)
- verifier minAmount === platform.getMinStake.minDelegator: **true**

## Live validators (8) — real NodeIDs, never invented

- `NodeID-BR9YCsrXGVsc8sUoraVrBkWemnPGRSqYZ` · window 2026-05-15T08:43:30.000Z → 2026-08-13T08:51:01.000Z (90.0d) · self-stake 5000000 FLR
- `NodeID-NKqPQeBNRT82BSHjFT9hCygtYQQBSBST7` · window 2026-08-06T14:12:44.000Z → 2026-10-07T10:00:00.000Z (61.8d) · self-stake 1000000 FLR
- `NodeID-87skCc745aTkJEtozqTJ2SBnZ8Vb8Ncbd` · window 2026-04-28T14:03:40.000Z → 2027-04-01T08:48:25.000Z (337.8d) · self-stake 40000000 FLR
- `NodeID-AKt7WaK6ozEy5K8azKNacZXLzxZ9xFgC7` · window 2026-04-13T08:43:42.000Z → 2027-04-08T06:09:48.000Z (359.9d) · self-stake 40000000 FLR
- `NodeID-FQKTLuZHEsjCxPeFTFgsojsucmdyNDsz1` · window 2026-04-15T08:16:50.000Z → 2027-04-13T06:25:54.000Z (362.9d) · self-stake 40000000 FLR

## Account state (honest empty / observed zero — nothing fabricated)

- P-chain staked positions: **0** — honest empty: readStakesOf returned [] — the account delegates to no validator (blank slate)
- mirrored vote power: 0 wei (0 FLR) — observed on-chain zero (a real balanceOf read, blank slate)
- staking reward: total **0**, claimed **0** (blank-slate 0,0)

## Plan gate + invariants against the LIVE limits (AC2-plan)

- chosen live validator: `NodeID-NKqPQeBNRT82BSHjFT9hCygtYQQBSBST7` (window ends 2026-10-07T10:00:00.000Z)
- **verified gate**: stakeVerified=false → refused `unverified` (no signable plan until a live round trip confirms)
- **valid intent** (under a verified OVERRIDE, not a source flip) → plan OK, value-lock: **50000 FLR**, locked **14 days**, unlocks 2026-08-26T19:45:22.000Z, irreversible=true
- **below 50,000 FLR** → refused `amount_below_min`
- **below 14 days** → refused `duration_below_min`
- **end past validator window** → refused `ends_after_validator`

## Funding shortfall (why the broadcast is HELD)

- native balance: **46.750559486830560487 C2FLR**
- live minimum: **50000 FLR** → shortfall **49953.249440513169439513 FLR**
- NOT fundable — native balance is far below the live 50,000-FLR minimum; the broadcast is HELD

## Carry

- The value-locking broadcast is HELD: the signer holds ~46.75 C2FLR ≪ the live 50,000-FLR minimum, and no explicit go was given. stakeVerified stays FALSE; the stake round trip is declared-unbuilt, nothing faked. It settles + flips only once the signer is funded ≥ 50,000 C2FLR AND Abu explicitly authorises the broadcast.
- honest-read expectations all passed: **true**
- The key is read locally ONLY to derive the public P-address (the probe pattern) — never signed with, logged, or written. Nothing is broadcast. The broadcast path is wired (`broadcast` subcommand) but hard-refuses on the funding gate.

---

## M11 close-out — T7 (mock) + T12 (gallery) + full gate (2026-08-13)

The value-locking broadcast is still CARRIED. M11 closes as **built-but-stake-carried**, the
plan's designed unfunded-close path (the M7 Firelight / M10 FTSO-reward / M8 MINT pattern) —
`stakeVerified` stays **false**, nothing faked.

### Funding re-verified live (keyless `eth_getBalance`, Coston2)

- signer `0xA4b05cdB545FA7CA12Be9f866d64E8A843A31Bd9`: **146.634056736830560487 C2FLR**
  (grew ~100 C2FLR since the 2026-08-12 probe's 46.75; still ≪ the live 50,000-FLR minimum)
- funded ≥ 50,000: **false** · shortfall **49,853.365943 FLR** → broadcast HELD

### T7 — `packages/core/src/mock-stake.ts` (copies observed, refuses unobserved)

- 176 lines (< 300). Fakes a P-chain `fetch` + an EVM `PublicClient` and runs the REAL read
  functions (`readValidators`/`readMinStake`/`readStakesOf`/`readStakeLimits`/`readMirrorVotePower`/
  `readStakingRewardState`) against them — no re-implementation, drives the true code path with no network.
- Honesty inversion from M10: the mock keeps the shipped `stakeVerified:false`, so the REAL
  `planStake` refuses `unverified` — it exposes NO signable/active stake. No config can inject a
  position; a non-coston2 network throws; a reverting mirror → `undefined` (unavailable), never `0n`.
- `mock-stake.test.ts`: **10 tests** — blank-slate reads match the observed snapshot; the honest
  deployment refuses `unverified`; the invariants hold under an explicit verified override; the
  observed empty position stays `awaiting_external`, never `succeeded`; mirror-unavailable ≠ zero.

### T12 — `packages/react-ui/gallery/m11-staking-sections.tsx` (observed states only, both themes)

- 10 StakeCard states + 2 ClaimCard staking-reward states, driven by the REAL `planStake` for
  every plan state. Browser-verified in both themes (screens below). Honesty confirmed on-screen:
  - `unavailable` → `Staked —` / `Mirrored vote power —` (a "Position unavailable" note), NEVER `0`.
  - blank-slate → `Staked 0.000000000000000000 C2FLR` / mirror `0` — a real observed zero, distinct.
  - the staking reward renders **"Does not expire"** — never the 25-epoch line; honest-empty note.
  - NO `succeeded` / active-stake case exists (the broadcast carried; nothing fabricated).
- a11y: **zero real violations** on the M11 surfaces. The one `not-measurable` flag is the shared
  primary `Button`'s gradient (identical flag on the accepted M9 section), not an `fk-stake-*` class.
- Screens: `.thoughts/verification/m11-screens/` — `m11-stake-{light,dark}.png`, `m11-reward-{light,dark}.png`.

### Full gate (repo root: `pnpm build && pnpm typecheck && pnpm lint && pnpm test`)

- build **4/4**, typecheck **9/9**, lint clean (`eslint .`), test **9/9**.
- tests: contracts 176 (+2 skip), core **962**, react-ui **385**, react **57**, relayer + x402-server pass. Zero failures.
- `publint`: **All good!** on contracts / core / react / react-ui. No `@flarenetwork/flare-tx-sdk`
  import in any shipped dist (the only `.d.ts` mention is a doc comment) — the runtime stays viem-only.

### Carried legs (declared, not dropped)

1. The value-locking stake **broadcast** — settles + flips `stakeVerified:true` only once the signer
   is funded ≥ 50,000 C2FLR AND Abu explicitly authorises it (then `live-stake.mjs broadcast`).
2. The delayed **P→C return** leg — becomes claimable after the stake's `endTime`.
3. The **staking-reward** claim — earned only after staking across epochs, then `ValidatorRewardManager.claim`.
