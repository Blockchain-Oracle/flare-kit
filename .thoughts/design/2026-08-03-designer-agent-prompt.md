# Prompt For Claude Fable 5

You own visual and interaction design for the complete Flare Application Layer. The commissioning
agent is the product-context holder, not the designer, and has deliberately selected no visual
language for you. Abu retains final taste authority.

## Delivery Contract

- Recipient: Claude Fable 5, external design agent.
- Workspace: `/Users/abu/dev/hackathon/flare`.
- Mandatory product packet:
  `/Users/abu/dev/hackathon/flare/.thoughts/design/2026-08-03-designer-commission.md`.
- Mandatory exact surface appendix:
  `/Users/abu/dev/hackathon/flare/.thoughts/design/2026-08-03-product-surface-map.md`.
- Ownership decision:
  `/Users/abu/dev/hackathon/flare/.thoughts/decisions/2026-08-03-visual-design-ownership.md`.
- Required output directory:
  `/Users/abu/dev/hackathon/flare/.thoughts/design/fable5-direction-return/`.
- First checkpoint: one coherent visual-language direction resolving the three fixed representative
  moments in the commission. Do not design all 112 surfaces yet.
- Later cadence: batches of three to four screens or coherent state groups only after Abu accepts
  the direction.

## Reading Order

1. Read the designer commission completely. It is the self-contained product contract.
2. Read the accepted product-surface map completely. It is the mandatory exact appendix for all 112
   surface IDs, universal/domain states, visible data, fixed fixtures, generated artifacts and copy
   laws.
3. Read the ownership decision for transfer and approval boundaries.

Do not reread or reinterpret the full research corpus. The commission already reconciles it. The
accepted specification and stories are optional audit sources only if you find an exact conflict in
the packet. If a mandatory file is unavailable, stop and ask the sender to attach these three files;
do not continue from this prompt alone.

## Product Boundary You Must Preserve

This is a complete Flare application layer across a full Build/Use/Automate/Operate/Verify
application, headless TypeScript, React, embeddable widgets, wallet/provider adapters,
policy-constrained agents, operator/support surfaces and release evidence.

Widgets are one integration format—not the application itself, the navigation root or the product
definition. Do not reduce the direction to a widget gallery, FXRP checkout or developer landing
page.

Treat every commission `LAW` as binding. Treat `CONTEXT` as accepted product understanding. Treat
`DIRECTION` as an overridable opportunity: when your design idea serves the LAWS and user jobs
better, yours wins.

You may propose connective additions marked `DISCOVERED`. Ask before proceeding if an addition
would change product scope, remove mapped coverage, change authority/privacy, alter supported-state
truth or contradict a LAW.

## First Return

Resolve the commission's fixed Moments A–C with identical fixtures:

1. The complete application shell with Build/Use/Automate/Operate/Verify, including the DC-1
   integrator-to-composed-widget path, a provenance-preserving portfolio and pending-operation
   continuity. The widget is visibly one delivery format inside the broader application.
2. The delayed 250 XRP → 247.500000 FXRP direct-mint operation, including the one-payment warning,
   FDC/executor wait, safe recovery and correlated evidence.
3. The human/agent shared bridge operation, including unsigned plan, deterministic policy result,
   exact approval, enforcement class and schema-equivalent receipt.

The return must demonstrate relevant loading, empty, degraded, pending, partial, error, recovery
and success states—not only final happy paths. It must show a resolved desktop application shell and
responsive/mobile action treatment without hiding amounts, authority, provider, public/private
boundaries or recovery risk.

## Required Files

Create:

- `fable5-direction-return/DIRECTION.md`
  - atmosphere and rationale tied to user jobs and product laws;
  - exact palette values and semantic roles;
  - typography and delivered font details;
  - spacing, density, radius, elevation and layout rules;
  - component/state grammar;
  - motion and reduced-motion rules;
  - responsive behavior;
  - product-derived signature elements and their semantics;
  - the direction's failure line—what would make it become generic or misleading.
- `fable5-direction-return/index.html`
  - portable interactive living sample for Moments A–C;
  - no production-integration claim;
  - no required runtime CDN for the transferred artifact.
- `fable5-direction-return/renders/`
  - desktop and mobile captures of every moment and required state.
- `fable5-direction-return/assets/manifest.md`
  - every font, icon and image; source, license, local file and acquisition requirement.
- `fable5-direction-return/HANDOFF.md`
  - run instructions;
  - exact interactions and covered states;
  - `DISCOVERED` additions;
  - assumptions, open questions and known gaps;
  - fixture fidelity statement;
  - concise audit notes another agent can follow without guessing.
- `fable5-direction-return/tokens.json` when token values are not already precisely machine-readable
  in `DIRECTION.md`.

## Design And Evidence Rules

- You own taste. No palette, typography, motif or component aesthetic was selected for you.
- The fixed sample values remain unchanged and visibly labelled `Illustrative`.
- A design-only state transition is not live protocol evidence.
- Never present submitted as succeeded, unknown as failed, or provider delivery as final
  application outcome.
- Preserve provider identity, exact values, signer/authority, irreversible boundary, expected wait,
  partial outcome, recovery risk and FCC public/private/trust distinctions.
- Documentation skills/docs MCP are read-only knowledge, never transaction authority.
- `0xFE` is not confidential; full calldata becomes public on Flare.
- Gasless does not mean free.
- Do not invent the exact FCC domain, production attestation, provider support, live addresses,
  prices or route superiority.
- Do not choose production architecture, framework, APIs, wallets, services or package structure.
  This return is design evidence, not implementation authorization.

## Accessibility And Return Quality

- Target WCAG 2.2 AA for kit-controlled surfaces.
- Provide keyboard/focus behavior, status semantics beyond color, readable order, dynamic-status
  announcement notes and reduced-motion behavior.
- Verify responsive layouts have no horizontal overflow and preserve decision-critical content.
- Record how longer localized strings and exact onchain values survive the direction.
- Provide real source and asset handback; screenshots without portable source are incomplete.

## Approval And Stop Conditions

The first return will be audited through the Direction Return Gate, then Abu will accept, request a
re-cut or reject it on taste. Delivery does not equal acceptance. Do not proceed to full surface
coverage until that decision.

Stop only when:

- a mandatory input file is unavailable;
- the commission and accepted surface map materially conflict; or
- the needed choice would change product scope, authority, privacy, supported-state truth or a LAW.

Otherwise make a strong design decision, document it in `HANDOFF.md`, and complete the first return.
