{
  "stage": "build",
  "stage_note": "M4's spec was accepted by Abu on 2026-08-04 as written, including the three carried debts (M4-R12, M4-R13, M4-R14) — he was offered a split to a fifth milestone and declined it. \"M4 done\" means all fourteen requirements, not the eleven FTSO ones. Source writes are open against the paths SPEC.md's manifest already admits.",
  "why": "M0 through M6 are complete and verified. M3 made the Flare Data Connector a first-class capability: one generic client over four attestation families, a catalogue that compares the built-in table against the live deployment, an attestation operation, and four surfaces. Scope is governed by the decision record, NOT by SPEC.md — SPEC.md is the M1 mint spec plus the repository-wide file manifest the scope guard reads. Without this marker a fresh session would read SPEC.md as total scope and conclude the project is nearly finished, when 19 capability families and 127 surfaces remain.",
  "milestone": "M12 GOVERNANCE COMPLETE — built AND live-verified on Coston2 (governanceVerified TRUE), real-first, no deploy. The delegate/undelegate ROUND TRIP LANDED on 2026-08-13 on Abu's explicit go: delegate 0xc0da39abf699242a1306c7ac659c59d7df98612940e8b4036ec6d0075d1419d7 (block 34007574) read back the target 0xDddF991858311597bFD3D125cb342a0d4B56ea0a via getDelegateOfAtNow, then undelegate 0x5537335d5fcabebcb512b9ece76f258b15cba773fd6a3785e0697e76e75bea7d (block 34007843) read back the ZERO address — round trip CLOSED, no residual delegation on Abu's account (reviewer re-verified live). No WNat wrap was needed: GovernanceVotePower.delegate sets a POINTER and succeeds at 0 governance VP. governanceVerified is TRUE for coston2 ONLY; flare mainnet stays FALSE (read lens, never flips). Governance VP is all-or-nothing to ONE address (delegate(to)/undelegate()) — NEVER M10's delegate(to,bips) two-provider FTSO split, and never conflated with IClaimSetupManager.delegateGovernance (a different contract). Proposals are an HONEST Flare-mainnet READ LENS: the one real proposal anywhere is FTSO id 1 \"Block-latency parameter changes\", DEFEATED (for 2354.308387975507843417 VP, against 0, threshold 6600 BIPS, majority 5000 BIPS, totalVotePower 5217.782567582675528275) — Coston2 is honest-empty and the foundation event scan found nothing on either network. castVote/propose/execute are BUILT AND CARRIED, never faked. portfolio's governance row flipped from declared-unbuilt to an observed position. NEXT: XRPL-controlled Smart Accounts.",
  "governing_decision": ".thoughts/decisions/2026-08-04-build-everything-real-first.md",
  "current_spec": ".thoughts/specs/2026-08-13-m12-governance.md",
  "handoff": ".thoughts/handoffs/2026-08-04-m3-complete-m4-ready.md",
  "next_authorized_action": "M12 (governance) COMPLETE and live-verified — governanceVerified TRUE on coston2, delegate/undelegate round trip closed with no residual delegation. Full gate green (build, typecheck, lint, test — contracts 183/2skip, core 1029, react 79, react-ui 423, relayer 7, x402-server 12); publint all-good; no new dependency entered any shipped surface; gallery browser-verified both themes with zero console errors and no M12 a11y findings; review gate applied (3 subagents + scoped re-review), 1 Critical + 5 Important found and fixed, re-verified. CARRIES (declared, NOT dropped): (1) the M12 castVote leg — planCastVote is built and gated but ALWAYS refuses {code:'carried'}: no Active proposal is executable on the write/verify network (Coston2 hosts none) and the account holds 0 mainnet governance VP, so no live cast is producible; (2) the M12 propose/execute legs (permissioned, declared-unbuilt); (3) the standing M11 value-locking stake BROADCAST (stakeVerified:false until the signer holds ≥50,000 C2FLR AND Abu authorises) + (4) the M11 P→C return leg + (5) the M11 staking-reward claim; (6) the M10 FTSO delegation-reward claim (rewardsVerified:false); (7) the M7 Firelight withdrawVerified flip; (8) the M8 cross-chain MINT shim. NEXT milestone in the governing decision: XRPL-CONTROLLED SMART ACCOUNTS — spec it next. INFRA follow-up (task #65, pre-existing and milestone-spanning): the react-ui gallery dir is OUTSIDE pnpm typecheck (tsconfig include is src/test/configs), so gallery prop-shape regressions escape the standing pipeline; M12 edited two gallery files. Two pre-existing errors must be fixed before gallery can join tsc — repo-wide decision, still tracked. CARRIED DEFECT (found by the M12 review gate, deliberately NOT fixed in M12): packages/core/test/delegation-states.test.ts:43 (M10) has the SAME illegal-edge fixture bug M12 fixed — it strands at draft with steps:[] so its spine assertions are vacuous. packages/core/test/governance-fixtures.ts is the repair template.",
  "read_first": [
    "CLAUDE.md",
    "DESIGN.md",
    ".thoughts/decisions/2026-08-04-build-everything-real-first.md",
    ".thoughts/handoffs/2026-08-04-m3-complete-m4-ready.md",
    ".thoughts/decisions/2026-08-04-m3-scope-fdc-only.md",
    ".thoughts/decisions/2026-08-04-no-first-party-proof-consumer.md",
    ".thoughts/specs/2026-08-13-m12-governance.md"
  ],
  "m4_inherits": [
    "FTSO-03's anchor-feed proof sits on the SAME data availability host as FDC and uses the same round derivation, finality poll and retrieval shape. All of that is built, live-verified and shared — packages/core/src/fdc/{round,client}.ts. That inheritance is the whole reason FTSO was deferred out of M3; do not restate any of it.",
    "The three unbuilt FDC items are now M4-R14: M4 supplies the DEFINITION each was blocked on, then builds it or re-declares it with the definition attached. They are FDC-04's `expired`, the `quota limited` states on FDC-02/03, and the source-aware asset for an EVMTransaction `value`.",
    "The two gaps carried since M2 are now M4-R12 and M4-R13 and close in this milestone: a WCAG 2.2 AA pass against computed styles across every surface M1–M4, and the state-by-state gallery audit of MintFXRP, RedeemFXRP and RecoveryPanel.",
    "The Coston2 signing account 0xA4b05cdB545FA7CA12Be9f866d64E8A843A31Bd9 held 98.989724 C2FLR on 2026-08-04, checked before accepting the spec. M4-R11's real offerIncentive costs ~0.37 C2FLR plus gas, so it is funded and needs no faucet trip."
  ],
  "completed_milestones": {
    "M0": "live kit, live chain-state reader, resume script deleted",
    "M1": "FAssets mint and redeem — live, in the UI, mock parity; evidence in .thoughts/verification/",
    "M2": "accounts, signing authority, portfolio and activity — six surfaces browser-verified, live portfolio read with no signing key; evidence in .thoughts/verification/",
    "M3": "FDC — one generic client over four families, all four driven live on Coston2 round 1415859 and verified on chain; four surfaces browser-verified; evidence in .thoughts/verification/",
    "M4": "FTSO surfaces — anchor feeds, feed catalogue, feed history, secure random, fast-update incentive; live on Coston2, offerIncentive run on chain; all 14 requirements incl. R12 WCAG-AA (computed styles) and R13 M1-surface audit; evidence in .thoughts/verification/",
    "M5": "Swaps — BlazeSwap V2 (Uniswap V2 router interface) on Coston2; SwapCard + SwapLeg + TokenSelector + SegmentedTabs, mock-swap.ts, gallery both themes; honest getAmountsOut quote, exact minReceived, approve→swap as two real steps; AC1/AC2/AC4 live 2026-08-09 (approve 0x9d2bfbc7…, swap 0x3fb03145…, both status success); evidence .thoughts/verification/2026-08-09-coston2-live-swap.md",
    "M6": "Liquidity — real add/remove on the live BlazeSwap V2 FXRP/USD₮0 pool (Coston2); AddLiquidityCard + PositionCard + PercentPills over the M1 operation lifecycle, mock-liquidity.ts written AFTER the live run (refuses unobserved), gallery all AC5 states both themes with a11y clean for every M6-new element. AC1/AC2/AC4 live 2026-08-11 — add 0x0200711d5fd9eea654e41850651d5832d138f5eb0c44df31b58a874503774474, approve-LP 0x2113295909cef73cd6aba57a0049b6853694cf760f7f6d79877aff0aec153210, remove 0x1576d74bf530d25f4673a630a9e5703c2db0a96b6634c6a3c40e0b73dd9d2177 (all status success; lpMinted 1112333 == predictedLp 1112333, exact). AC5 browser-verified. ROOT-CAUSE: BlazeSwap addLiquidity is NON-STANDARD (extra feeBipsA/feeBipsB) — caught by the real-first run before the mock copied it. Whole-branch review resolved: F1 PositionCard `unavailable` (unknown read never rendered as a confident \"no position\"); F2 add quote refuses unverified-venue adds (registry addLiquidityVerified flag — Coston2 only — so a mainnet add never signs approvals then reverts); R10 PoolCatalogue BUILT as a declared-unbuilt surface (present/disabled/reasoned, fk-unbuilt idiom); R7 exact-entry added beside 25/50/75/Max, and value-change-vs-supplied-basis renders a price-free per-asset delta when the kit recorded the basis (nothing when unknown — no invented P&L). Full gate 1072 passed / 2 skipped / 0 failed; every new element a11y-clean both themes. Evidence: .thoughts/verification/2026-08-11-m6-liquidity.md (+ 2026-08-11-coston2-live-liquidity.json, m6-screens/**).",
    "M8": "Cross-chain (LayerZero V2 OFT bridge + FAssetRedeemComposer redeem) — Coston2 ↔ Sepolia ↔ XRPL. @flare-kit/contracts bridge.ts route registry (per-route bridgeVerified) + bridge-abis.ts (IOFT/dest-read/Composer, split from abis.ts). @flare-kit/core: bridge-adapter.ts (ONE CrossChainAdapter over a SOURCE and a DESTINATION viem client), bridge-options.ts (hand-rolled type-3 executor/compose option encoder, no LZ runtime dep, validated byte-for-byte against a live quoteSend), bridge-quote.ts (honest fee/dust-adjusted receive/slippage floor), bridge.ts + bridge-states.ts (verified-gated plan → durable submit→await-delivery→delivered lifecycle self-reconciling against the DESTINATION chain; 3-leg redeem executor→flare→xrpl), mock-bridge.ts (copies observed, refuses unobserved). @flare-kit/react use-bridge.ts (delivery poll). @flare-kit/react-ui RouteCatalogue + BridgeCard (+ bridge.css), reusing SwapLeg/OperationTimeline/card-chrome. Gallery m8-bridge-sections.tsx: every AC5 state, both themes, a11y-clean (M8-new elements). LIVE 2026-08-11 (evidence .thoughts/verification/2026-08-11-m8-cross-chain.md + coston2-live-bridge.json + m8-bridge-probe.json + m8-screens/): AC1 bridge 1 FXRP Coston2→Sepolia DELIVERED and confirmed by reading Sepolia (send 0xdea75d2d…50c4, guid 0xd0f1e2c6…2b22a04, Sepolia balance 0→1_000_000, quoted==delivered exact); AC1 redeem 10 FXRP Sepolia→Coston2 composer→native XRP SETTLED on XRPL 9.94 XRP (send 0x11638b57…, FAssetRedeemed 9_990_000 UBA, XRPL tx 401F7591…, agent rDYeq…). succeeded reached ONLY from the destination read (bridge) / the XRPL settlement read (redeem). Both Coston2 routes bridgeVerified:true. Cross-chain MINT ships declared-unbuilt (M8-R9). Full gate green: build+typecheck+lint+test (contracts 137/2-skip, core 754, react 22, react-ui 308). Review gate: 3 subagents (correctness / honest-rendering-silent-failure / simplification); all critical+important fixed and tested — terminal reconcile clears stale `awaiting` and finalizes spine steps (advanceSteps), XRPL settlement correlated on amount+timing, buildPlan returns `unavailable` on a read failure, useBridge clears a transient read error, delivery-leg projection honest for every state, slippage clamped; simplifications: pathTo homed in states.ts (shared with FDC), dead BridgeState/sharedDecimals/underlyingToken removed, quoteReceive narrowed, floorMin DRY'd.",
    "M9": "Gasless, relayers, payments - two real payment rails, live-verified on Coston2, real-first. (1) GASLESS FXRP: a custom GaslessPaymentForwarder (0x7F358717afdEC6FD4AFEfCf2e7dD9ff3dF4b9c17, ADAPTED to enforce the authorizedRelayers allowlist the reference example left decorative) + a one-time FXRP approval + a fee-free operated relayer (services/relayer). AC1/AC2 live 2026-08-12: a real FXRP payment landed with the payer paying 0 GAS FOR IT - approval payer-gassed (tx 0x753a184f770f1b745b5cc0698bcf540833b35cc73689cadf3583233682137ca5, payer C2FLR -0.0335), payer drained to ~0.001365 C2FLR, relayed payment tx 0x799d3c9eb0893643d418f334cb15d97cc6b2140d541bd46cef6901938f5b02d4 (payer C2FLR UNCHANGED across the payment; FXRP payer -1 / recipient +1); succeeded reached ONLY from the on-chain PaymentExecuted read; gaslessVerified:true. (2) x402/HTTP-402: MockUSDT0 (labelled demo token, 0x2dA725841FF6F5367E65C5d114aa66C034A3d97b) + X402Facilitator (0x57da665Ef6Bd39F82Af6BC0764cd779E9C156DdA, deployed FEE-FREE) self-deployed on Coston2 + services/x402-server (single-endpoint fixture, not a marketplace). AC3 live 2026-08-12: 402 -> sign EIP-3009 -> settle -> resource; settlement tx 0x2923aa74b5478b3cf6b5aa6e279acfdf120ece201afd8de8f0701d076e698158 (status success, mUSDT0 payer -0.1 / payee +0.1) and the HTTP 200 resource recorded as TWO INDEPENDENT facts; asset labelled a demo token throughout; succeeded only from settled+delivered; x402Verified:true. All 3 contracts explorer-verified; reference/contracts is a standalone Hardhat project (NOT a workspace member). @flare-kit/contracts: gasless.ts/gasless-abis.ts/x402.ts/x402-abis.ts (registries, *Verified flags, demoToken:true; no address literal outside the registries). @flare-kit/core: gasless-eip712/gasless-adapter/gasless/gasless-states/mock-gasless + x402-eip3009/x402-client/x402-states/mock-x402 + reconcile.ts (shared table-walk helpers reconcileTo/waitSince/advance EXTRACTED from bridge-states, now shared by bridge/gasless/x402); the EIP-712/EIP-3009 crypto is defined ONCE in core and imported by the services (no drift). services/* is a new workspace category (relayer + x402-server, both import @flare-kit/core; keys from env, never logged/returned). @flare-kit/react: use-gasless/use-x402 (durable reconcile polls reusing useBridge). @flare-kit/react-ui: GaslessCard + X402Card (+ pure state splits) + payments.css + the shared LegTimeline primitive, reusing SwapLeg/OperationTimeline/card-chrome; gallery both themes, browser-verified, a11y-clean on every M9-new element. Full gate green (contracts 148/2-skip, core 811, react 28, react-ui 321, relayer 7, x402-server 12). REVIEW GATE (correctness / security / honest-rendering-silent-failure / simplification subagents): 1 CRITICAL + several important, all fixed and re-tested - the shipped x402 server AND the relayer did not check receipt.status, so a mined-but-REVERTED settlement (the double-settle race) was dressed as succeeded (the live scripts had the check; the shipped code did not); the x402 server settled WITHOUT binding the authorization `to` to the payee (free-resource bypass) and had an unguarded async handler (unauthenticated DoS); the inline leg-timeline was extracted to the shared LegTimeline. Mocks copy observed, refuse unobserved. Evidence: .thoughts/verification/2026-08-12-m9-gasless-payments.md (+ 2026-08-12-m9-gasless.md, 2026-08-12-m9-x402.md, coston2-live-gasless.json, coston2-live-x402.json, m9-probe.json, m9-screens/**).",
    "M10": "Delegation + claims on the live Coston2 Flare contracts, real-first, no deploy. DELEGATION: @flare-kit/contracts delegation.ts/rewards.ts registries (WNat reuses the wrappedNative snapshot; delegationVerified/rewardsVerified flags) + IWNat/IVPToken/RewardManager/RNat/Distribution/FlareSystemsManager ABIs; @flare-kit/core delegation-adapter/delegation/delegation-states (verified-gated plan, <=2 delegates / Sum bips <=10000 / percentage-vs-explicit mode-exclusivity, succeeded only from delegatesOf) + portfolio.ts flip (delegation placeholder -> observed|unavailable, stake still unbuilt) + mock-delegation. LIVE 2026-08-12: wrap(0x68d6bb35…c572) -> delegate 0xB63C1E02…38B5D(0x7b8fa4e1…681a) -> undelegate(0x70ed6dbb…7d3b) -> unwrap(0x8b4903ad…13f0); delegationVerified:true after the confirmed delegatesOf read. Two live-run fixes: dev-script record-shape; WNat.withdraw OOG (cold vote-power-checkpoint SSTORE under-estimated by eth_estimateGas right after undelegate — simulate-clean, exec-reverted; fixed w/ explicit gas). CLAIMS: three distinct kinds (rewards-adapter/rewards/rewards-states + mock-rewards) — FTSO (Merkle proof from unofficial mirror gitlab timivesel, official:false; 25-epoch expiry; claimableEpochs gated on rewardsHash!=0; getStateOfRewards is tuple[][] 5-field RewardState distinct from claim()'s 4-field RewardClaim), rNat (getBalancesOf reverts 'no RNat account'->hasProject:false honest-empty; withdrawAll burns 50% of locked, shown before signing), FlareDrop (getClaimableMonths reverts 'already finished'->concluded 2026-01-30). Reward reads live keyless; FTSO claim CARRIED (rewardsVerified:false). @flare-kit/react use-delegation/use-rewards; @flare-kit/react-ui DelegationCard + ClaimCard (one component, 3 distinct kinds) + delegation.css; gallery all 26 states both themes, M10-new a11y-clean. Gate green: contracts 168/2skip, core 899, react 49, react-ui 360. Spec .thoughts/specs/2026-08-12-m10-delegation-claims.md; evidence .thoughts/verification/2026-08-12-m10-delegation-claims.md (+ live JSON, rewards md, m10-screens/).",
    "M11": "M11 STAKING built-but-stake-carried on Coston2, real-first (no deploy). Keyless reads landed live 2026-08-12 (block 33979980, signer 0xA4b05cdB…31Bd9, P-costwo1mxmg9…hnmnacj): 8 validators (real NodeIDs, never invented), live limits min 50,000 / max 200,000,000 FLR + 14–365 days, verifier.minStakeAmount === getMinStake.minDelegator, blank-slate account (readStakesOf [], mirror balanceOf 0, getStateOfRewards (0,0)). @flare-kit/contracts staking registry (stakeVerified:false) + PChainStakeMirror/Verifier/ValidatorRewardManager ABIs + parity. @flare-kit/core: pchain-rpc (getCurrentValidators/getMinStake/getStake reads + bech32 P-address, nFLR→wei ×1e9), stake-adapter (EVM reads readStakeLimits/readMirrorVotePower/readStakingRewardState + StakeIntent + PChainStakeExecutor SEAM — no flarejs under any src), staking.ts (planStake invariants + StakeValueLock, discriminated Result), stake-states (reconcileStake: succeeded only from readStakesOf; return leg absent-only-from-confirmed-read; unavailable never succeeds on silence), portfolio stake flip (declared-unbuilt while unverified), mock-stake (copies observed, refuses the unobserved — keeps stakeVerified:false so the REAL planStake refuses `unverified`; drives the real readers over fake fetch/PublicClient; 10 tests), staking reward = 4th ClaimKind (expires:false, honest-empty where total===claimed). @flare-kit/react use-staking (keyless reads/plan, injected executor). @flare-kit/react-ui StakeCard (live-read validators + bounds, value-lock pre-sign, 4-leg spine, unavailable→—) + staking.css + ClaimCard staking kind (non-expiring). Gallery m11-staking-sections: 11 StakeCard states + 2 reward states, both themes, a11y-clean, screens in .thoughts/verification/m11-screens/ (blank-slate 0/0 vs mirror-unavailable 0/— vs position-unavailable —/— all rendered distinctly). Gate green: contracts 176/2skip, core 962, react-ui 385, react 57, relayer 7, x402-server 12; publint all-good; no flarejs in shipped dist (only a doc-comment mention). Review gate applied — correctness clean, simplification lean, honest-rendering invariants hold; fixed a reachable mirror-only-unavailable gallery gap (M1), a mirrorUnavailable comment (M2), and an M11-caused m10-claims RewardsReads type drift (T8 made `staking` required). CARRIED: the value-locking broadcast (stakeVerified FALSE; signer 146.63 C2FLR ≪ 50,000-FLR min) + the P→C return + the staking-reward claim. Spec .thoughts/specs/2026-08-12-m11-staking.md; evidence .thoughts/verification/2026-08-12-m11-staking.md (+ …-m11-coston2-live-stake.json, …-m11-probe.json, m11-screens/).",
    "M12": "M12 GOVERNANCE complete and LIVE-VERIFIED on Coston2 (governanceVerified TRUE), real-first, no deploy — the four governance contracts were resolved BY NAME from FlareContractRegistry on BOTH networks (GovernanceVotePower, PollingFoundation, PollingFtso, PollingManagementGroup; GovernorReject confirmed ABSENT, legacy v1), zero drift, and GovernanceVotePower cross-checked against WNat.governanceVotePower(). LIVE ROUND TRIP 2026-08-13 (signer 0xA4b05cdB…31Bd9): delegate 0xc0da39ab…19d7 block 34007574 → getDelegateOfAtNow read back 0xDddF9918…ea0a; undelegate 0x5537335d…bea7d block 34007843 → read back ZERO. Closed and reversible; no wrap needed (the delegate pointer sets at 0 governance VP). @flare-kit/contracts: governance registry (coston2 verified:true / flare false) + three hand-curated ABIs + parity asserted against a LIVE getAllContracts read on both networks. @flare-kit/core: governance-adapter (VP + eligibility reads, undefined-on-throw never a fabricated zero; isMember is boolean|undefined because PollingFtso.isMember REVERTS for non-members, surfaced via allSettled and NEVER coerced to false; pure single-target call builders), governance.ts (verified-gate FIRST, then invalid_target / self_delegation / already_delegated / no_delegate invariants), governance-states (reconcileGovernance walks the canonical states.ts table via the shared reconcileTo — succeeded ONLY from the getDelegateOfAtNow read-back, case-insensitive, never from the submission), proposals.ts + proposal-mapping.ts (bounded eth_getLogs event scan + getLastProposal; TWO per-contract state enums dispatched on a source discriminator; discovery THROWS on a failed read so an outage can never wear the shape of a confirmed-empty catalogue), portfolio governance flip, mock-governance (copies observed, refuses unobserved). @flare-kit/react: use-governance (keyless reads/plan, injected walletClient, tx hash recorded as flare_tx evidence) + use-proposals (undefined = unavailable, [] = confirmed-empty, kept distinct end to end). @flare-kit/react-ui: GovernanceCard + governance-card-state + governance.css, ProposalCatalogue, ProposalDetail, proposal-visuals — single-target composer (no bips field anywhere), unavailable → \"—\" distinct from an observed 0, four distinct catalogue availability states, FTSO-absent fields (votePowerBlock/hasVoted/accountVotes) → \"—\" with the reason, totalVotePower carried honestly and never relabelled circulatingSupply, castVote present but disabled and carried. Gallery m12-governance-sections: 22 cases across 3 sections driven through the REAL core path (both settled cases are the SAME record differing only in read-back), both themes, browser-verified at 1440px, zero console errors, a11y zero M12 contrast/target-size findings (M12 inputs measure 410x39; the 3 target-size failures are all m5-token-selector). Gate green: contracts 183/2skip, core 1029, react 79, react-ui 423, relayer 7, x402-server 12; publint all-good; NO new dependency (viem-only, zero package.json changes across the milestone). REVIEW GATE (3 reviewers + a scoped re-review) found and fixed 1 Critical + 5 Important: a swallowed FTSO discovery read that returned [] and rendered as \"the discovery read succeeded — nothing failed\"; a missing already_delegated guard that reached succeeded off PRE-EXISTING state; a test fixture that jumped an illegal edge so four spine assertions were VACUOUS (including the one guarding dropped patches); a discarded tx hash leaving the declared evidence slot empty; stale rows re-stamped with a new network's label; and a hardcoded \"Flare mainnet\" note title. Spec .thoughts/specs/2026-08-13-m12-governance.md; evidence .thoughts/verification/2026-08-13-m12-governance.md (+ …-m12-probe.json, …-m12-governance-reads.json, m12-screens/)."
  },
  "rules_that_are_not_obvious": [
    "Real integration first; the mock is written afterwards and copies observed behaviour.",
    "The docs site and apps/app come LAST — they consume the packages, so building them early hides which package does not work.",
    "Nothing is dropped for time. If it cannot be built to the bar it ships declared unbuilt.",
    "Never cd into sources/ — the harness resolves the project root from the shell cwd, and the stage guard will block source writes.",
    "Never cd into a package directory: the stage guard resolves the project root from the shell cwd, so `cd packages/core` makes it infer stage=orient and block every source write. Use pnpm --filter from the root.",
    "The test lock unlocks one edit PER TEST FILE per SPEC.md write — it compares each file's mtime to SPEC.md's, so one SPEC.md write unlocks every test file, but only once each. Batch test edits behind one real SPEC.md change; never touch SPEC.md just to unlock.",
    "SPEC.md's '## Files' section ends at the next heading of ANY depth — a ### subheading inside it hides everything below from the scope guard.",
    "react-ui imports @flare-kit/react from dist, so a hook change needs pnpm build before react-ui tests see it.",
    "applyTransition SILENTLY DROPS ITS PATCH when the transition is not allowed by the table in states.ts. A reconciler that jumps states loses its step updates with no error and no failing test — fdc/operation.ts walks the table with a breadth-first search for exactly this reason. Any future reconciler must do the same.",
    "Relay.isFinalized returning true does NOT mean the proof can be fetched. The data availability layer indexes the round minutes later. Poll retrieveProof; never conclude 'no proof' from a single absence.",
    "The FDC request fee is 1000 wei on Coston2 and 20 FLR on mainnet (3 FLR for ConfirmedBlockHeightExists). Never hardcode it.",
    "The verifier's OpenAPI and the on-chain fee configuration are two independent oracles for what a deployment serves, and they disagree about Web2Json on mainnet. Checking only the route would report an unsubmittable family as supported.",
    "FTSO: FtsoV2.verifyFeedData REVERTS on a bad proof where FdcVerification.verifyEVMTransaction returns false. The two protocols must NOT share a boolean verification abstraction — catching the revert and coercing it to false renders 'we could not check this' as 'this is not proven'.",
    "FTSO: decimals are per READING, not per feed. FLR/USD is 6 on the anchor path and 8 on the block-latency path. Never cache decimals per feed and never share them between the two paths.",
    "FTSO: the anchor-feed DA returns HTTP 200 with an EMPTY ARRAY for an unknown feed id, and only 400 for a non-retained round. fdc/client.ts checks status only, which is correct for FDC and a silent failure here. Check array length and per-feed presence.",
    "FTSO: the Relay's Merkle root OUTLIVES the DA's leaves, so committed-on-chain-but-no-longer-retrievable is a real first-class state. BUT the recorded floor of 1130919 / ~297 days is WRONG — measured 2026-08-05, round 1130919 retrieves a value and the boundary is around 818368, roughly twice the window.",
    "FTSO: enumerate with getSupportedFeedIds(), never getFeedIds() — the latter is a storage array with recycled slots and returns 64 entries including an unused zero feed id at index 52.",
    "FTSO: a custom feed id is 0x21 ++ ASCII name right-zero-padded to 21 bytes. developer-hub/docs/ftso/guides/create-custom-feed.mdx:75,147 documents 0x21 + keccak256(name) and is WRONG — every deployed feed disproves it.",
    "FTSO: reads are payable and free TODAY but that is not a property of the surface — fetchAllCurrentFeeds() already costs 1 wei because the unused slot falls through to defaultFee(). Quote calculateFeeByIds; never assume zero.",
    "FTSO: a Fast Update incentive's effect DECAYS. Confirmation must read getRange() at the transaction's own block against the preceding one, not a later block. A late read showing no effect is not a failed offer.",
    "FTSO: getFeedsById SILENTLY RESOLVES A RENAMED ID to its current feed — getFeedsById([MATIC/USD, POL/USD]) returns 74335 for BOTH on Coston2. Labelling a reading with the id you asked for prints POL/USD's price under the name MATIC/USD. readFeeds resolves via getFeedIdChanges and carries requestedFeedId + formerName. Any future read path must do the same.",
    "FTSO: a payable read needs an ACCOUNT, not just a value. simulateContract with value and no account runs as from=0x0, and go-flare checks balance >= value, so it fails with insufficient funds. The fee is 0 on Coston2 today, which hides this completely.",
    "FTSO: viem's readContract has NO value parameter — passing one is silently dropped. Payable views must go through simulateContract or the call goes out with 0 and reverts wherever the fee is non-zero.",
    "FTSO: the anchor DA does NOT preserve request order, non-deterministically — two identical requests returned opposite orders. Match entries by body.id, never by position; a positional zip swaps one feed's price with another's, intermittently.",
    "FTSO: only `voting_round_id` selects a DA round. votingRoundId, round_id and no param all return HTTP 200 carrying the LATEST round. Always compare the returned votingRoundId against the requested one.",
    "FTSO: getCustomFeeds() is NOT the custom feed list — undeclared in every vendored interface, returns a category-0x00 entry that reverts on read, and omits stFLR/USD. Derive custom feeds by filtering getSupportedFeedIds() on category 0x21.",
    "FTSO: two fee oracles disagree. FtsoV2.calculateFeeByIds says 0 for mainnet's 66 feeds; FeeCalculator says 3 (category 0x21 has no configured fee, falls through to defaultFee 1 wei). Quote from the contract that GUARDS the call — FtsoV2.getFeedsById accepts value=0.",
    "FTSO: decimals vary WITHIN one batch, not just across paths. One live getFeedsById returned 8 (FLR/USD), 9 (SGB/USD) and 2 (BTC/USD). Zip the decimals array positionally against the response; never look one up by feed id.",
    "FTSO: the anchor retention floor is NOT MEASURABLE by bisection, and the recorded 1130919 / ~297 days is WRONG — round 1130919 retrieves a value today. Two bisections back-to-back in one run, each confirming every absence THREE times, returned boundaries 176,168 rounds apart (885160 and 1061328). Cause: the data-availability host intermittently serves an EMPTY 200 for a round it demonstrably holds. So: NEVER conclude a round is gone from one absence, and never report a floor. Query per round. readFeedHistory now confirms an absence 3x before classifying, because committed_not_retrievable is a PERMANENT claim that a committed value is gone forever and it was previously reachable from one blip. Evidence: .thoughts/verification/2026-08-05-live-ftso-flare-testnet-coston2.md",
    "FTSO: the incentive offer price is rangeIncrease * rangeIncreasePrice / (getPrecision() * 64). Pricing against getRange() — the obvious reading — overstates by ~5500x (2000 FLR instead of 0.37). Fitted to four live bisections; verifyOfferAmount dry-runs the real call before signing because the divisor is empirical, not derived.",
    "FTSO: no vendored interface maps a custom feed id back to its contract address (IICustomFeed only goes address->id), so IICustomFeed.calculateFee/getCurrentFeed are unreachable. Custom feed values come from FtsoV2.getFeedsById; the direct read ships declared, not faked.",
    "A unit test written against a wrong formula passes. The incentive price bug was only caught by driving the live contract — for anything priced, bisect against the chain and put the measured numbers in the test as fixtures.",
    "@flare-kit/react does NOT depend on @flare-kit/contracts. Core re-exports the FTSO id vocabulary (FeedId, FEED_CATEGORY, encodeFeedId, decodeFeedId, isCustomFeedId) so consumers can name a parameter type without a second dependency.",
    "mock-ftso.ts is a READER the real core functions are driven by, not a second implementation. It also refuses to answer anything it never observed, so an unmocked call is a loud error rather than a plausible zero. It is the only way to test the paid-read path, since the Coston2 fee is 0.",
    "DEX/BlazeSwap: the router addLiquidity is NOT standard UniswapV2 — it inserts two extra uint256 params, feeBipsA and feeBipsB (fee-on-transfer tolerance, 0 for standard tokens), BETWEEN amountBMin and `to`. The standard 8-param selector (0xe8e33700) hits the router fallback and reverts with EMPTY data. removeLiquidity and swapExactTokensForTokens ARE standard. Authoritative signature: sources/flare-foundation/fasset-bots/packages/fasset-liquidator/contracts/interfaces/IUniswapV2/IUniswapV2Router.sol; Flare's own liquidator passes feeBipsA=0, feeBipsB=0. Caught only by the live add run (empty-revert + a sibling call working = an ABI/selector mismatch, not a logic bug). NOTE: a future mainnet (SparkDEX V2) add path may use the STANDARD signature — adds are venue-specific, unlike swap/remove, so the shared UNIV2_ROUTER_ABI can't serve both networks for addLiquidity when that path is built.",
    "DEX: a V2 add mints min(amountA*ts/rA, amountB*ts/rB) LP (Uniswap V2 mint()), NOT amountA*ts/rA — with real reserves the two differ by ~1 wei from floor-division, so predicting lpFromA overstates by a wei and breaks predicted==minted. The kit's quoteAddLiquidity uses the min; the live run confirmed lpMinted == predictedLp exactly. LP token decimals for a V2 pair are 18 (UniswapV2ERC20 constant), independent of the pool tokens' decimals; expected LP renders as an Amount at 18dp (this BlazeSwap pair's LP symbol is BLAZE-LP).",
    "VAULTS/Upshift: deposit is NON-standard — deposit(address assetIn, uint256 amountIn, address receiver), asset-address FIRST (not ERC-4626 deposit(assets,receiver)); previewDeposit(asset,amount) and previewRedemption(shares,isInstant) return TWO values incl. a contract-computed assetsAfterFee. The kit renders assetsAfterFee (contract-computed) as the honest net received, per route. The share/LP token is a SEPARATE ERC20 at lpTokenAddress() (vFXRP on Coston2, earnXRP on Flare mainnet — probe the symbol, never guess). Withdraw/instant PULL the LP so they need an LP approve; Firelight self-share redeem needs none.",
    "VAULTS/Firelight: the standard ERC-4626 maxDeposit RETURNS uint256 MAX and does NOT reflect the vault custom GLOBAL depositLimit — a deposit within maxDeposit still reverts DepositLimitExceeded() (0x6adf7e28) once totalAssets reaches depositLimit. Measured live 2026-08-11: depositLimit 15000000, totalAssets 14994061 → only 5939 units (0.005939 FXRP) of real headroom. availability() must cap at min(maxDeposit, depositLimit-totalAssets). Firelight withdraw/redeem are DELAYED period-based requests (no immediate transfer); claimWithdraw(period) after currentPeriodEnd. The FIRST redeem in a period does extra checkpoint writes — buffer gas ~1.5x or it goes out-of-gas (an estimate taken right after the deposit undercounts; the tx replays fine via eth_call, confirming gas not logic).",
    "VAULTS/lifecycle: a vault delayed withdrawal needs NO new operation state — it maps onto the existing canonical states awaiting_external (period/epoch clock) -> action_required (claimable, via escalate with a safe claim action, movesNewValue:false) -> succeeded. Same shape as M3 FDC proof wait. withdrawVerified (per vault, per network) is the M7 analog of dex.ts addLiquidityVerified: buildWithdrawPlan refuses an unverified withdraw path without reading or approving anything.",
    "M8/LayerZero: Coston2's RPC caps eth_getLogs to ~30 blocks, so a delivery reconcile that scans from the submit block over a multi-minute wait MUST chunk into ≤25-block ranges or the read reverts. Sepolia tolerated a wide range and hid this; chunking is correct on both. bridge-adapter.ts scanByGuid does it.",
    "M8/FAssets redeem: FAssetRedeemed on the composer is the redemption REQUEST filed, NOT 'XRP received' — the FAssets agent pays XRPL separately (same as M1's redeem: nothing is done until the protocol settles). The op reaches succeeded ONLY from the XRPL settlement read. The reconciler has THREE legs, all awaiting_external distinguished by actor: executor (LZ delivery) → flare (FAssets filed) → xrpl (agent paying). A first cut that concluded succeeded from FAssetRedeemed alone was the milestone's own honesty class and was caught in review.",
    "M8/lifecycle: a reconciler that advances `awaiting` but never advanceSteps leaves a succeeded op's spine steps 'pending', which OperationTimeline draws as 'outcome unknown' — and the spine renders on succeeded (card-chrome IN_FLIGHT includes succeeded). Terminal reconciles MUST finalize steps AND clear the descriptor with {awaiting: undefined} — applyPatch only clears a key that is EXPLICITLY present, so an undefined whole-patch leaves a settled op still claiming it waits on somebody.",
    "M8/XRPL settlement correlation: the redeemer's XRPL address also receives faucet drops, so matching 'the most recent incoming Payment' false-positives the redeem success. Correlate on delivered_amount ≈ redeemedAmountUBA (net of the agent fee, e.g. ≥95%) AND a ledger date that postdates the redemption filing.",
    "M8/lot size: the cross-chain redeem's FAssets leg is lot-based (1 lot = 10 FXRP on Coston2, lotSizeXRP 10). Verifying it live needs ≥ 1 lot on the SOURCE chain first — a 1-FXRP bridge cannot be redeemed. Phase A bridged 1 FXRP (the AC1 gate); a top-up bridged 9 more before the redeem.",
    "M8/OFT: the type-3 executor option OMITS the value word when it is 0 (16-byte lzReceive payload, 18-byte lzCompose payload). The FXRP OFT route removes NO dust (sharedDecimals 6, decimalConversionRate 1) so amountReceivedLD == amountSentLD; a stale RPC once returned 0 balance for a funded Sepolia account (the never-conclude-from-a-single-absence rule again).",
    "M7/Firelight: a delayed redeem in period P files its withdrawal under period P+1 (the UNLOCK period), and claimWithdraw(P+1) is callable only once currentPeriod > P+1. Recording the REQUEST period P strands the claim: withdrawalsOf(P)=0 and claimWithdraw(P) reverts (0x95dece8d). The claim ref period is currentPeriod+1, claimable at nextPeriodEnd (read at request time). Encoded in firelightClaimRef(); caught live 2026-08-12 when the Phase-A ref recorded period 333 but the withdrawal was at 334. Upshift is calendar-epoch, NOT off-by-one.",
    "M9/EIP-712: the deployed forwarder domain ('GaslessPaymentForwarder','1') and MockUSDT0 domain ('Mock USDT0','1') are byte-identical to their on-chain DOMAIN_SEPARATOR; core reproduces both OFFLINE (viem hashDomain, asserted in tests). That domain-separator EQUALITY is a far stronger 'single source of truth' test than a sign->recover round-trip, which passes even when the domain is self-consistently wrong.",
    "M9/receipts: viem waitForTransactionReceipt RESOLVES for a mined-but-REVERTED tx (it only rejects on timeout/replacement). A settle/relay that passes the pre-send simulateContract can still revert when mined (the double-settle race: two requests with the same EIP-3009 authorization both pass verify+simulate; the first consumes the nonce, the second reverts 'authorization used'). Gate success on receipt.status==='success', or a reverted settlement is dressed as settled->succeeded. The M9 live scripts had the check; the SHIPPED server/relayer did NOT - caught by the honesty review subagent, not by any test.",
    "M9/x402: the x402 server MUST bind the authorization's `to` to the configured payee before settling (settlePayment is unbound, unlike the facilitator's settlePaymentAsPayee). Without it a payer signs a self-transfer (from=to=attacker), the facilitator settles a net-zero transfer, and the server hands over the paid resource for free (the M9 security review's payee-bypass).",
    "M9/services: Express 4 does NOT catch a rejected async handler - an unauthenticated malformed input (a bad X-Payment header) becomes an unhandled rejection that can crash the process. Wrap service handlers in try/catch AND strictly type-validate decoded payload fields (not just truthiness) before BigInt()/toLowerCase() touch them.",
    "M9/viem: passing a bare ADDRESS STRING as simulateContract({account}) makes viem treat it as a node-signed (json-rpc) account, so writeContract(sim.request) then goes out via eth_sendTransaction and a node without the key rejects it ('Missing or invalid parameters'). Pass the LOCAL Account object (walletClient.account) so the request local-signs (eth_sendRawTransaction).",
    "M9/gas: Coston2 base gas spiked to ~650 gwei during the live run (~26x the usual ~25). A native-transfer drain-to-~0 must compute its fee from an explicit legacy gasPrice + gas=21000, not an EIP-1559 maxFee estimate, or the tx fails 'gas required exceeds allowance' (the remaining balance covers 21000*gasPrice but not 21000*maxFeePerGas).",
    "M9/gasless-not-free: the one-time approve(forwarder) is the ONLY step the payer pays gas for; every payment after it is gasless (the relayer bears the gas). The proof is 'payer NATIVE balance UNCHANGED across the payment', not 'payer balance is 0' - the payer never sends the payment tx so its balance is untouched regardless; draining to ~0 is corroborating, not the proof.",
    "M9/EIP-3009 substrate: neither FXRP nor the real Coston2 USD-T0 dispatches any EIP-3009 selector (re-probed 2026-08-12), so x402 has no real-token substrate on testnet - it runs on a self-deployed MockUSDT0 whose demoToken flag is REGISTRY DATA (never a hand-typed label that could drift), and the client derives 'is this a demo token' from the registry by ADDRESS, never from the server's (untrusted) asset string.",
    "M9/reconcile: reconcileTo/waitSince/advance now live in packages/core/src/reconcile.ts, shared by the bridge/gasless/x402 reconcilers - the applyTransition-silently-drops-its-patch hazard is handled in ONE place. Any new capability reconciler imports these, never re-implements the table walk.",
    "M10/Flare delegation: `delegate` != delegation — a delegate op reaches `succeeded` ONLY from the on-chain `delegatesOf` read reflecting the target, NEVER from the send. Undelegate succeeded = delegatesOf empty; wrap succeeded = wrappedBalance reflects the wrap.",
    "M10/Flare: percentage vs explicit (AMOUNT) delegation are MODE-EXCLUSIVE — a percentage `delegate` while the account is in AMOUNT mode (or vice-versa) SILENTLY NO-OPS on-chain. The plan refuses it (mode-conflict) and the card reads 'undelegate first', never emitting the no-op call. `delegationModeOf` NEVER resets to NOTSET once set, so exercising AMOUNT on a single signer permanently blocks its PERCENTAGE path.",
    "M10/Flare: WNat.withdraw writes a COLD vote-power-checkpoint SSTORE that `eth_estimateGas` UNDER-COUNTS right after an undelegate — the tx simulates clean but the mined tx OOG-REVERTS (status:'reverted', not a decode error; happened twice 300 blocks apart). FIX: set an explicit generous gas limit on WNat/vote-power-checkpoint-writing calls. Simulate-clean != execution-clean. (Carry to M11 staking — same WNat/vote-power surface.)",
    "M10/Flare rewards honesty: RNat.getBalancesOf REVERTS 'no RNat account' (not (0,0,0)) and DistributionToDelegators.getClaimableMonths REVERTS 'already finished' (FlareDrop concluded 2026-01-30). A CONTRACT REVERT maps to a DECLARED honest-empty state; a TRANSPORT/network error must RETHROW (unknown != empty — a dead RPC must never render as 'no rNat').",
    "M10/Flare rewards shapes: getRewardEpochIdsWithClaimableRewards returns a (start,end) RANGE tuple, NOT a list; getStateOfRewards returns tuple[][] whose inner RewardState has 5 fields incl. `bool initialised` — DISTINCT from claim()'s 4-field RewardClaim (no bool). claimableEpochs must be gated on FlareSystemsManager.rewardsHash(epoch)!=0 (the signed-epoch gate). Delegation-reward expiry is 25 epochs; staking rewards do NOT expire — never one 'rewards expire' line.",
    "M10/Flare: the Coston2 FTSO delegation-reward proof is an UNOFFICIAL community mirror (gitlab.com/timivesel/ftsov2-testnet-rewards, layout rewards-data/coston2/<epoch>/reward-distribution-data.json; amount serialized as a trailing-'n' STRING — strip before BigInt). proofSource.official:false rides every surface; absent tuples -> declared proof-unavailable, never a fabricated proof. rNat withdrawAll burns 50% of locked — shown as real value destruction before signing.",
    "M10 process: a verified-flag flip (delegationVerified false->true) BREAKS gate tests whose fixture used the registry DEFAULT — use an EXPLICIT verified/unverified fixture, and after ANY flip re-run the FULL suite (not just the flipped package). Run ROOT `pnpm lint` in the gate: zero-width-space chars + script no-undef (e.g. AbortController in .mjs) only surface there, not in per-package build/typecheck/test.",
    "M10/Flare: resolve a live FTSO provider read-only via VoterRegistry.getRegisteredVoters(rewardEpochId) ranked by votePowerOf — the getter takes uint256 (not uint24) and the ACTIVE epoch reverts inside the policy-snapshot window, so use epoch+1.",
    "M11/mock honesty-inversion: a mock for a CARRIED (unverified) round trip must keep `*Verified:false` so the REAL plan builder refuses (`unverified`) — it exposes no signable/succeeded state. This is the OPPOSITE of mock-delegation (M10), which legitimately flips delegationVerified:true because that round trip WAS driven live. Never flip a verified flag in a mock to make a composer look active when the broadcast carried; drive the real gate against the honest-false deployment instead. Corollary: the gallery must render every prop-reachable state — the mirror-only-unavailable (readStakesOf [] but balanceOf reverted → Staked 0 / mirror —) is distinct from whole-read-unavailable (—/—)."
  ],
  "m4_progress": {
    "gate": "build+typecheck+lint green, 960 tests (contracts 111+2 skipped, core 623, react-ui 208, react 18)",
    "done": "M4-R1..R10 complete, R9 included: every path that reaches a surface now travels as Observation<T> and renders through SourceChip. R7's real submission is still R11. P7 shipped the six surfaces; the R9 anchor+history half landed after it. P8's read-only half is run and recorded.",
    "still_unbuilt": [],
    "hooks": "useFeeds/useFeedCatalogue, useFeedHistory, useAnchorProof, useSecureRandom, useIncentiveOffer, useCustomFeeds, all over use-observed-read.ts",
    "p7_lessons_for_p9": [
      "TOGGLING data-theme FROM SCRIPT AND MEASURING IN THE SAME TASK LIES. It reported the dark ghost button at 1.05:1 — near-black on near-black — which is not real: the custom property changed without dependent `color` values recomputing. Through the gallery's own toggle the same button is rgb(241,240,237), correct. A dark-theme bug was nearly 'fixed' that never existed. Always drive the application's real state change.",
      "RESOLVED 2026-08-05: the waiting/unknown glyph collision. They were the same dashed ring separated only by `opacity: 0.6`. Abu chose the DOTTED ring for `unknown`; the opacity is gone. All seven glyphs now differ by shape and none is dimmed — verified in-browser. DESIGN.md's vocabulary line was updated, and it now states the rule explicitly: seven marks, seven SHAPES, no state told apart by colour, weight or opacity alone.",
      "M4-R12's contrast pass MUST composite opacity. Confirmed in-browser 2026-08-04: getComputedStyle().color returns the DECLARED colour and never folds in `opacity`, so a checker reading it directly passes every dimmed element. Measured on `.fk-src-age` (pre-existing M2, opacity 0.8): declared reads 4.67:1 and PASSES; correctly composited it is 3.36:1 and FAILS AA. The tell that a checker is broken is two elements of different opacity reporting an identical ratio — an earlier P7 sweep reported 4.46:1 for both .fk-ts and .fk-ts-zone when only the latter was dimmed.",
      "Verified contrast method for M4-R12: walk ancestors multiplying `opacity`; composite the text colour over its effective background in sRGB BYTE space BEFORE linearising (the browser's order — compositing in linear space is off by ~0.5); linearise with the WCAG piecewise curve, not gamma 2.2; resolve the background by walking ancestors for the first non-transparent one. Sanity-check the implementation against one element of known opacity before trusting any output.",
      "KNOWN R12 FINDINGS already measured: `.fk-src-age` at opacity 0.8 composites to 3.36:1 and fails AA — pre-existing, affects every M2/M3 surface carrying a SourceChip. `.fk-skeleton` is at opacity 0.55 and is decorative (aria-hidden), so it is exempt. `.fk-ts-zone`'s opacity was removed in P7.",
      "packages/react-ui/src/ProofDetail.tsx is 328 lines, still the only production file over CLAUDE.md's 300-line cap. M4-R14 already owns splitting it.",
      "The shape signature in test/ftso-shapes.tsx reads chip WORDS, empty-row copy and skeleton counts as well as data-* attributes, because DESIGN.md requires every state to carry a glyph AND a word. Two states differing only in body prose still collide, deliberately.",
      "mock-ftso.ts now REFUSES a feed id it never observed rather than filling 0n at 0 decimals. It previously rendered `0 USD` as the price of three real mainnet custom feeds. Only sFLR/USD (1128983 at 8 decimals) has a measured custom value; stXRP/USD and stFLR/USD were enumerated, never read.",
      "Panel forwards arbitrary data-* onto its root, so a surface state can be a named state rather than existing only as a paragraph. IncentiveComposer uses data-blocked, SecureRandomPanel data-stale/data-refused.",
      "The P7 design audit found four required states unreachable from props and one surface claiming a check that never happened. Both classes are invisible to typecheck, tests AND screenshots — they need a reader asking 'what prop combination reaches this state?' Run the design-auditor on every surface batch, and read its findings before declaring a phase done."
    ],
    "open_for_p9": [
      "`.fk-fdc-catalogue` is applied at react-ui/src/AttestationCatalogue.tsx:132 and DEFINED NOWHERE. Measured: it computes to display:block with no gap, while its FTSO twin is flex-column gap:12px — M3 compensates with margins on .fk-fdc-provenance instead. Not broken, and M3 was browser-verified as it stands, so it was NOT changed: either fix shifts a verified surface's spacing or drops a targeting hook. Decide deliberately.",
      "css-integrity.test.ts checks defined-CSS-class -> referenced-in-TSX but NOT the reverse, which is how the above survived a green suite. Add the second direction: every `fk-` class a component applies must have a rule, with an explicit allowlist for scope-only hooks. This is the check that would have caught it.",
      "The live-bisected incentive constants (RANGE_INCREASE, RANGE_INCREASE_PRICE, PRECISION) are duplicated in THREE places: core/test/ftso-random-incentive.test.ts, react-ui/gallery/m4-incentive-sections.tsx and react-ui/test/ftso-fixtures.ts. These are measured protocol facts, not fixture choices, so they can drift silently and a stale copy would still look plausible. Home them in mock-ftso.ts, which already owns 'what was observed'.",
      "react-ui/test/ftso-fixtures.ts exports COSTON2, POL_USD and SOURCE, which nothing imports. Cosmetic; blocked by the test lock during P7."
    ],
    "p8": "COMPLETE. Read half recorded; Abu ran the real offerIncentive himself 2026-08-05 — tx 0x542f15d2f07ad7502b8f198c47a525249207182918d866539823ab889b8eec54, block 33654765, 366210937499999999 wei, M4-AC7 satisfied (measured range delta identical to the event's rangeIncrease, sampleSizeIncrease 0). A decode bug reported confirmed:false first — topics[1] is rewardEpochId (5902), not rangeIncrease, because only that field is indexed on IncentiveOffered. Corrected from the same receipt via decodeEventLog, no re-spend. LESSON: decode events through the ABI, never by indexing topics.",
    "r12": "COMPLETE 2026-08-05. Zero contrast/target/focus failures both themes, every surface M1-M4. Checker lives at packages/react-ui/gallery/a11y-audit.ts, exposed as window.__auditA11y() and re-runnable. Fixed: --fk-text-faint darkened (was 4.48/4.46 against `surface`, failing AA across 18 classes; DESIGN.md's published ratios were computed against `bg`, the most flattering background), .fk-src-age opacity removed (3.36:1 composited), .fk-ev-link min-height 24px (was 40x18). Evidence: .thoughts/verification/2026-08-05-m4-r12-accessibility.md",
    "r13": "AUDITED 2026-08-05, seven findings fixed, the rest carried with reasons. Evidence: .thoughts/verification/2026-08-05-m4-r13-m1-surface-audit.md. BIGGEST: the gallery's `.g-section h2` beat `.fk-panel-title` on specificity, so EVERY M3/M4 panel screenshot showed the wrong heading and uppercased asset symbols — fixed with `>`. Also fixed: an expired recovery window rendered as 'nothing is at risk'; a paused asset manager rendered as 'Amount too small'; the XRPL memo absent from the last screen before signing; four exact values in the body face; RecoveryPanel missing its `fk` scope class and reading Date.now() in render. CARRIED: C1 (no seed prop, so AC7's refusal has never been seen in a browser), C3 (quote expiry; RedeemQuote has no expiresAt at all), I1/I3/I5/I11 and the minors. C1, C3 and I1 CLOSED 2026-08-05: seed props added, RedeemQuote gained expiresAt, both composers take `now` and disable on stale terms, and the M1 gallery now has 12 cases each reaching a distinct state. AC7's refusal is rendered in a browser for the first time. GALLERY CLOCK GOTCHA: the composers quote through the mock kit at MOCK_EPOCH, not wall time — a hardcoded date AND Date.now() both read as expired on every case. Use MOCK_EPOCH.",
    "smaller_items": "DONE 2026-08-05. (1) css-referenced-defined.test.ts adds the direction css-integrity never checked — every `fk-` class a component APPLIES must have a rule. It found THREE undefined FDC surface roots where the audit found one: fk-fdc-catalogue, fk-fdc-builder and fk-fdc-proof all computed to display:block with no gap while their FTSO twins were flex-column. All three now share the one stack rule in data.css; verified in-browser. fk-sr-row and fk-table-skeleton are allowlisted with reasons — they sit on <tr> where a stack rule would break table layout. (2) The live-bisected incentive constants now live once, in mock-ftso.ts as OBSERVED_INCENTIVE_PRICING — they are measured protocol facts, and a stale copy of one still looks plausible. (3) The unused ftso-fixtures exports remain; cosmetic, blocked by the test lock, not worth a SPEC write.",
    "glyph_vocabulary": "RESOLVED 2026-08-05. The seven marks are an OPERATION-OUTCOME vocabulary; only outcomes carry them. `ToneChip`'s glyph is now optional and a label chip (mock kit badge, `Renamed`) takes its word alone — both previously borrowed `unknown`, which means outcome-unknown, so a label claimed something it cannot know. An eighth 'this is a label' mark would have inflated a contract DESIGN.md calls fixed. Recorded in DESIGN.md. Verified: 19 label chips carry no mark, and every chip that still does names an operation outcome."
  },
  "protocol_gotchas_m7": [],
  "m7_firelight_settled": "2026-08-13: Firelight delayed-withdraw CLAIM settled live — claimWithdraw(period 334) tx 0xbba94b139057e33ba955fde908f41c30d1fc6a6f1302a6c74edfc6a1ecf3c71d, status success, block 33998606, received 3000 (0.003 FXRP); phase verified 'claimable' pre-sign; evidence .thoughts/verification/2026-08-11-coston2-live-vault.json phaseB.firelight. The request->claim round trip is PROVEN. withdrawVerified flip DEFERRED: firelight-fxrp is the suite's canonical declared-unbuilt fixture (contracts vaults.test.ts:65, core vault.test.ts:103 'REFUSES an unverified vault', + react-ui/mock/gallery), so setting it true needs its own reviewed cross-package migration + browser re-verify. Kept withdrawVerified:false (safe under-claim) with an evidence-cited comment in vaults.ts until that migration runs. NOTE: an earlier hasty flip was reverted after it reddened the vault suite; a background 'exit 0' notification was misleading — trust test OUTPUT, not exit codes.",
  "protocol_gotchas_m12": [
    "GovernanceVotePower is a DIFFERENT contract from M10 FTSO vote power. delegate(to) is all-or-nothing to ONE address; there is no bips form. It is also NOT IClaimSetupManager.delegateGovernance, which is a claim-executor feature on another contract entirely.",
    "GovernanceVotePower.delegate sets a POINTER and succeeds at 0 governance VP — no WNat wrap or balance is required to move the delegate, which is why the M12 round trip cost nothing but gas.",
    "PollingFtso.isMember REVERTS for a non-member address rather than returning false. Any eligibility read must surface it as undefined; coercing the revert to false invents a negative answer the chain never gave.",
    "There are TWO different proposal-state enums, and they disagree at the same index. FTSO (IPollingFtso.sol): Canceled=0, Pending=1, Active=2, Defeated=3, Succeeded=4. Foundation (IGovernor.sol): Pending=0, Active=1, Defeated=2, Succeeded=3, Queued=4, Expired=5, Executed=6, Canceled=7. Index 3 means Defeated on FTSO and Succeeded on Foundation — decoding with the wrong table inverts the outcome. Dispatch on a source discriminator.",
    "The deployed PollingFtso getProposalInfo shape carries NO votePowerBlock, NO hasVoted and NO per-voter getVotes. Those fields must stay undefined and render as \"—\"; the vendored IGovernor interface is NOT the deployed FTSO shape.",
    "Proposal discovery must respect the Flare eth_getLogs range cap and page in bounded windows. Real foundation proposals are MONTHS apart while any sane scan window is hours, so a bounded event scan legitimately returns nothing on mainnet — getLastProposal is the only read that actually surfaces the live proposal.",
    "An empty array from proposal discovery is a CLAIM (\"the read succeeded and the chain has none\"), not the absence of an answer. A catch that swallows a failed read into [] therefore fabricates a statement about the chain even though it fabricates no data. M12 shipped this bug and the review gate caught it: discovery now throws and the hook keeps undefined (unavailable) distinct from [] (confirmed-empty).",
    "A no-op write reaches succeeded off PRE-EXISTING state. Re-delegating to the address you already delegate to makes the read-back match immediately, so the reconciler confirms an operation whose transaction may never have landed. Refuse no-ops at the PLAN, where the pre-state is known, not at the reconciler, which cannot tell two identical reads apart.",
    "applyTransition DROPS its patch on an illegal state hop and returns the record unchanged. A test fixture that jumps an illegal edge therefore silently produces steps:[], and every steps.every(...) assertion written against it is VACUOUSLY true — including assertions written specifically to guard dropped patches. Fixtures must walk the legal path and assert they arrived."
  ]
}
